<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error 404 - Page Not Found</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      .container {
        flex: 1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 40px 20px;
      }
      h1 {
        font-size: 4rem;
        color: #2563eb;
        margin-bottom: 0.5em;
        font-weight: 700;
        letter-spacing: 2px;
      }
      p {
        font-size: 1.2rem;
        color: #334155;
        margin: 0.5em 0;
      }
      a {
        color: #2563eb;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s;
      }
      a:hover {
        color: #1e40af;
        text-decoration: underline;
      }
      .contact {
        margin-top: 1.5em;
        font-size: 1rem;
        color: #64748b;
      }
      @media (max-width: 600px) {
        h1 {
          font-size: 2.5rem;
        }
        .container {
          padding: 20px 10px;
        }
      }
    </style>
  </head>
  <body>
    <%- include("includes/nav.ejs") %>
    <div class="container">
      <h1>Error 404</h1>
      <p>Sorry, the page you are looking for does not exist.</p>
      <p>Please check the URL or return to the <a href="/">home page</a>.</p>
      <p class="contact">If you believe this is an error, please contact Satish Medar.</p>
    </div>
    <%- include("includes/footer.ejs") %>
  </body>
</html>
